#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

// for N lines we have to answer Q queries


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int n, q;
    cin >> n >> q;

    for (int i = 0; i < n; i++) {
      // work with each line here

      // each line EITHER has an opening tag with zero or more attributes, OR a closing tag
    }

    for (int i = 0; i < q; i++) {
      // handle each query here
    }


    return 0;
}
